# http://travis-ci.org/#!/graphite-project/carbon
language: python
python:
  - 2.6
  - 2.7

env:
  - TOXENV=py26
  - TOXENV=py27
  - TOXENV=lint

install:
  - pip install tox

script:
  - tox -e $TOXENV
